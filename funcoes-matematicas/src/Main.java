        // Raiz quadrada
            // a = Math.sqrt(x);
        // Ao quadrado
            // a = Math.pow(x);
        // Valor absoluto (sem negativo)
            // a = Math.abs(x);
        // Mais informações -> java.lang.Math